model:
  default: MiniMax-M2.7

mcp_servers:
  github:
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-github"]
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: ""  # 请填入你的 GitHub PAT
    timeout: 60